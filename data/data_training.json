[
    {
        "question": "Is this a block banner?",
        "answer": "If there are multiple <div class='home-slider-item swiper-slide'> containing <a> and <img> tags, this is a block banner.",
        "logic": "Check if there are multiple items in that block?. Create a variable to store the banner location with a random name, for example: BANNER_HOME.. If there are multiple items in that block, define variable: {% set banner_home = getBannerByPositionCode('BANNER_HOME') %}, If there is only one item in that block, define variable: {% set banner_home = getCurrentBannerByCode('BANNER_HOME', {'limit':1}) %}. Check if the declared banner is not empty then render HTML"
    },
    {
        "question": "How to display banner list?",
        "answer": "Use the loop to render the list banner",
        "logic": "In a banner there are the following attributes: viewLink, target, imageSrc, name, description, intro. Use {% for b in banner_home %} to loop through each banner. The banner attributes correspond to the tags described above (If <a> tag set: href = {{ b.viewLink }}, target = {{ b.target }}, aria-label must be {{ b.name }}, title must be {{ b.name }}. If <img> tag: set src = {{ b.imageSrc }},alt  must be {{ b.name }})",
        "example": "{% set banner_home = getBannerByPositionCode('BANNER_HOME') %} {% if banner_home is not empty %}\n    {% for b in banner_home %}\n        <a {{ b.target }} href='{{ b.viewLink }}' title='{{ b.name }}'>\n            <img loading='lazy' src='{{ b.imageSrc }}' alt='{{ b.name }}'>\n        </a>\n    {% endfor %}\n{% endif %}"
    },
    {
        "question": "Is this a block products list?",
        "answer": "If there are multiple <div class='product-item'> containing: <a>, <img>, <button> tags this is a block product. Furthermore, the product block also has price, discount price, and discount percentage.",
        "logic": "Check if it is a product block, will create a variable to call the function to get the product list, for example: {% set newProduct = searchProducts({'limit':10}) %}. Variable 'newProduct' automatically generate block product"
    },
    {
        "question": "How to display products list?",
        "answer": "Use the loop to render the list products",
        "logic": "Trong một sản phẩm có các thuộc tính sau: id, productUnit, code, price, oldPrice,contactPrice, priceAfterDiscount, calcDiscountPercent, viewLink, imageUri, typeId. Sử dụng {% for p in newProduct%} để lặp qua từng sản phẩm. Các thuộc tính sản phẩm tương ứng với các thẻ được mô tả ở trên (Nếu thẻ <a> được đặt: href = {{ p.viewLink }}, aria-label phải là {{ p.name }}, title phải là {{p.name }}, tạo thêm thuộc tính data-id phải là {{ p.id }}, tạo thêm thuộc tính data-type phải là {{ p.id typeId}} Nếu thẻ <img>: đặt src = {{ p.imageUri}}, alt phải là {{ p.name }}). Nếu có biểu tượng phần trăm thì hãy kiểm tra {% if(p.calcDiscountPercent > 0) %} {{ p.calcDiscountPercent }} % {% endif %}. Nếu có giá, hãy kiểm tra giá theo bên dưới: Nếu giá = 0: phải để text là liên hệ, Nếu p.priceAfterDiscount > 0: thì lấy giá p.priceAfterDiscount  là giá gốc, giá khuyến mãi là p.price, Nếu p.oldPrice> 0: thì lấy giá p.price là giá gốc, giá khuyến mãi là p.oldPrice, Còn không thì mặc định lấy ra giá gốc : p.price",
        "example": "{% set newProduct = searchProducts({'limit':10}) %}\n{% if(newProduct is not empty) %}\n{% for p in newProduct %}\n{% set priceDiscount = p.priceAfterDiscount %}\n<a href=\"{{ p.viewLink }}\" title=\"{{ p.name }}\" class=\"_productItem\" data-id=\"{{ p.id }}\" data-type=\"{{ p.typeId }}\">\n<img loading=\"lazy\" src=\"{{ p.thumbnailUri }}\" alt=\"{{ p.name }}\">\n{{ p.name }}\n{% if(p.calcDiscountPercent > 0) %}\n{{ p.calcDiscountPercent }} %\n{% endif %}\n{% if(p.contactPrice or (p.price == 0)) %} Liên hệ\n{% elseif (p.priceAfterDiscount > 0) %}\n Discount price: {{ p.priceAfterDiscount|number_format(0) }}\n Old Price: {{ p.price|number_format(0) }}\n                                        {% elseif (p.oldPrice > 0) %}\n                                            Discount price: {{ p.price|number_format(0) }}\n                                            Old Price: {{ p.oldPrice|number_format(0) }}\n {% else %}\n Price: {{ p.price|number_format(0) }}\n{% endif %}\n</a>\n{% endfor %}\n{% endif %}"
    }
]